# Clustering Section

## Dataset

The MNIST dataset will be loaded using scikit-learn's `fetch_openml` or similar method.

## Files

- `clustering.ipynb` - Jupyter notebook implementing all clustering tasks (max 50MB)
- `report_clustering.pdf` - Clustering report (max 3 pages, convert to PDF for submission)

